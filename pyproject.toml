[project]
name = "trust-me-i-am-a-cert-auth"
version = "0.1.0"
description = "Trust me, I'm a Certificate Authority"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=50.0.0",
    "flask>=3.1.3",
    "requests>=2.34.2",
    "waitress>=3.0.2",
]
